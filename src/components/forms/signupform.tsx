import { useState } from 'react';
import { NewUserInterface } from '../../types/userTypes';
import React from 'react';

const SignupForm = () => {
    const [newUser, setNewUser] = useState({} as NewUserInterface);
    return <div></div>;
};
export default SignupForm;
